{
  "name": "hospital-management",
  "version": "1.0.0",
  "dependencies": {
    "chart.js": "^4.5.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "dev": "npx tailwindcss -i ./css/style.css -o ./css/tailwind.css --watch",
    "build": "npx tailwindcss -i ./css/style.css -o ./css/tailwind.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
